import { describe, it } from "vitest";

describe("Button", () => {
  it("renders", () => {});
  it("handles click", () => {});
  it.skip("supports drag", () => {});
  it.only("focused test", () => {});
  it("is accessible", () => {});
});
