export const CastInput = forwardRef((props, ref) => null) as ForwardRefComponent;

const CastButtonBase = forwardRef((props, ref) => null) as ForwardRefComponent;

export default memo(CastButtonBase) as MemoComponent;
