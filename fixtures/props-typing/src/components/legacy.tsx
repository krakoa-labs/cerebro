export class LegacyModal extends Component<LegacyModalProps> {
  render() {
    return null;
  }
}

const ThemedBoxBase = (props: ThemedBoxProps) => null;
export const ThemedBox = withTheme(ThemedBoxBase);

export const Surface: ComponentType = (props) => null;
