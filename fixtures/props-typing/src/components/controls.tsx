export function Button(props: ButtonProps) {
  return null;
}

export const Input = forwardRef<HTMLInputElement, InputProps>((props, ref) => null);

export const Select = forwardRef((props: SelectProps, ref) => null);

export const IconButton = forwardRef<HTMLButtonElement>((props, ref) => null);

export const Checkbox = (props) => null;
