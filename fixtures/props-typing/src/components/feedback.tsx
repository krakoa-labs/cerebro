export const Toast = (props: ToastProps = {}) => null;

export const Badge = ({ count }: BadgeProps) => null;

export const Tag = (props: { label: string }) => null;

export const Banner = memo((props: BannerProps) => null);

export function Spinner() {
  return null;
}

export const Alert = ({ message }) => null;
