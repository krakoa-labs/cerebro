export const Card = (props: CardProps) => null;

export const Panel: FC<PanelProps> = (props) => null;

export const Sheet: React.FC<SheetProps> = (props) => null;

export function Divider(props) {
  return null;
}
