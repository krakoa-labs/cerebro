export const Inline = memo(({ children }: { children: ReactElement }) => null);

export const Code = memo((props: { children: string }) => null);

export const Untyped = memo((props) => null);

export const Spinner = memo(() => null);
